# Optional external Supabase credentials (if connecting to an external Supabase project)
# By default, the application uses the built-in Cloud SQL PostgreSQL database
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
